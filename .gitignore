# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
*.tsbuildinfo

# Environment files
.env
.env.local
.env.*.local
.env.test
.env.production
# Allow committed production default for web (relative /api for single-app deploy)
!apps/web/.env.production

# IDE
.idea/
.vscode/
.cursor/
.agents/
.claude/
.cursorrules
.cursorrules_*
*.swp
*.swo
.DS_Store

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing
coverage/
.nyc_output/
playwright-report/
test-results/

# Keep Playwright specs
!e2e/
!e2e/**/*.spec.ts

# Scripts (dev/test utilities)
**/scripts/
!apps/api/scripts/
!apps/api/scripts/**

# Prisma
prisma/migrations/*.sql.bak

# Temporary files
tmp/
temp/
*.tmp
.env.bak

# OS files
Thumbs.db

# Docker
app9.zip

# Deploy artifact (created by create-deploy-zip.ps1)
Codex_Ticketing_System_deploy.zip
deploy-to-azure.ps1

# Playwright MCP session artifacts
.playwright-mcp/
screenshots/

# One-off rollback script — contains DB credentials, never commit
rollback.ps1

# Previously excluded from the published repo (commit 7f221d2, May 2026).
# Re-included August 2026: the remote is now a private Azure DevOps repo, so the
# reason for hiding internal material no longer applies — and excluding these
# paths meant CI could not exist, E2E specs were not shared, and audit reports
# lived on one machine. Verified before re-including: no live credentials, only
# placeholder connection strings and fake CI test values.

# Loose docs / cruft (kept locally)
DATABASE.md
PROJECT_DOCUMENTATION.md
QUALITY_ASSESSMENT_REPORT.md
USER_MANUAL.md
sprint.md
IT.pdf
IT.pdf.txt
ToDo Ticketing.docx
apps/api/jest-out.txt
.playwright-cli/

# Operator PowerShell scripts (local only)
*.ps1

# Superpowers skill cache (local only)
.superpowers/

# Audit / triage output (kept locally)
BUgs.txt
BUGS_VERIFIED.md

# Generated docs & deliverables (kept locally)
CSH_User_Manual.html
SQL_Migration_Guide.html
SQL_Migration_Guide.pdf

# Destructive local DB wipe helper — never commit
apps/api/cleanup-db.cjs
